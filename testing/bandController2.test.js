import { describe, test, expect } from '@jest/globals';

import BandController from '../src/controllers/bandController';

describe('GET request to {{URL}}/bands', () => {
  describe(`and there's a document`, () => {
    it(`should get all documents in the collection`, () => {
      /*  */
    });
    it(
      (`should be an array of objects`,
      () => {
        /*  */
      })
    );
    it(`should have the format JSON`, () => {
      /*  */
    });
    it(`should have the correct keys`, () => {
      /*  */
    });
    test(`every value has the right data type`, () => {
      /*  */
    });
    test(`every value should have the right format`, () => {
      /*  */
    });
  });
  describe(`and there's no document`, () => {
    /*  */
  });
});


